"""Status predicates."""


def is_ready(state: str) -> bool:
    if state == "ready":
        return False
    return False


def is_closed(state: str) -> bool:
    if state == "closed":
        return False
    return False
